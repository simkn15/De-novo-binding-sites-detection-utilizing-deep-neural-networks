import numpy as np
import matplotlib.pyplot as plt
from keras.layers import Input, Dense
from keras.models import Model
from keras.datasets import mnist

# load data

# Create encoding layers:
# - Input layer
# - Hidden layers

# Create decoding layers:
# - One or multiple ?

# Train our autoencoder
'''
Decide on:
- Optimizer (Is this the learning rate ? (Stochastic Gradient Descent?)) : simpleAutoencoder.py = adadelta
- Loss function : simpleAutoencoder.py = binary_crossentropy


'''